export interface TemplateType {
  duration: number
  id: number
  dependency: number
}

const durationAlgorithm = (templates: TemplateType[]): number => {
  const duration = 0;

  return duration;
};

export default durationAlgorithm;